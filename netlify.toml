[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA-friendly fallback (không bắt buộc với site 1 trang, nhưng an toàn nếu
# sau này thêm route con như /projects/:slug)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
