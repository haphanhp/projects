{
  "name": "project-dashboard",
  "version": "1.0.0",
  "description": "Markdown-driven project dashboard: checklists, completion %, git activity heatmap.",
  "type": "module",
  "scripts": {
    "data": "node scripts/build-data.js",
    "dev": "npm run data && vite",
    "build": "npm run data && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  }
}
